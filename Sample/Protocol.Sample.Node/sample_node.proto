syntax = "proto3";

package proto.Node.sample;

// ===== Node (Service Mesh RPC) Messages =====

// Stateless Node Echo RPC
message ServiceMeshEchoReq
{
    string message = 1;
}

message ServiceMeshEchoRes
{
    string message = 1;
    int64 node_id = 2;
    int64 timestamp = 3;
}
